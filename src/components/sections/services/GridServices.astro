<section class="border-t border-dashed border-white/15 bg-linear-to-t to-amber-900/20 via-transparent from-transparent text-white py-20">
  <div class="container mx-auto px-6">

    <!-- Header -->
    <span 
      data-reveal
      class="text-samana-1 text-xs font-mono tracking-[0.4em] uppercase"
    >
      Nuestros Servicios
    </span>

    <h3 
      data-reveal-text
      class=" text-4xl md:text-5xl text-shadow-2xl text-shadow-black font-bold mt-6 mb-20 leading-tight"
    >
      Creamos espacios funcionales y duraderos para vos.
    </h3>

    <!-- Grid con stagger -->
    <div class="grid md:grid-cols-3 gap-8 reveal-group">

      <!-- Servicio -->
      <div class="relative h-[420px] bg-neutral-900 overflow-hidden group mask-reveal">
        
        <!-- Imagen futura con parallax -->
        <div class="absolute inset-0 parallax scale-110">
          <!-- Aquí luego colocas imagen o video -->
        </div>

        <!-- Overlay animado -->
        <div class="absolute inset-0 bg-black/40 transition-all duration-700 group-hover:bg-black/60"></div>

        <!-- Contenido -->
        <div class="absolute bottom-6 left-6 mask-content">
          <h4 class="text-xl font-semibold tracking-wide">
            Reformas Integrales
          </h4>
        </div>
      </div>

      <!-- Servicio -->
      <div class="relative h-[420px] bg-neutral-900 overflow-hidden group mask-reveal">
        <div class="absolute inset-0 parallax scale-110"></div>
        <div class="absolute inset-0 bg-black/40 transition-all duration-700 group-hover:bg-black/60"></div>
        <div class="absolute bottom-6 left-6 mask-content">
          <h4 class="text-xl font-semibold tracking-wide">
            Venta de Materiales
          </h4>
        </div>
      </div>

      <!-- Servicio -->
      <div class="relative h-[420px] bg-neutral-900 overflow-hidden group mask-reveal">
        <div class="absolute inset-0 parallax scale-110"></div>
        <div class="absolute inset-0 bg-black/40 transition-all duration-700 group-hover:bg-black/60"></div>
        <div class="absolute bottom-6 left-6 mask-content">
          <h4 class="text-xl font-semibold tracking-wide">
            Diseño & Renderizado
          </h4>
        </div>
      </div>

    </div>

  </div>
</section>
